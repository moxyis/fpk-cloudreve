name: 版本比对后自动打包Cloudreve并更新仓库

on:
  workflow_dispatch:  # 手动触发
  schedule:
    - cron: '0 0 * * *'  

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 授予写入仓库权限
      pull-requests: read
    steps:
      # ===================== 第一步：环境准备 =====================
      - name: 1. 检出当前仓库代码（带完整历史）
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 完整检出历史
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 2. 配置Git全局身份（跨仓库推送通用）
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: 3. 安装基础依赖（jq/curl/sed）
        run: |
          sudo apt update && sudo apt install -y jq curl sed
          echo "基础依赖安装完成"

      # ===================== 第二步：版本比对（核心前置） =====================
      - name: 4. 读取本地manifest版本号
        id: local_ver
        run: |
          if [ -f "Cloudreve/manifest" ]; then
            LOCAL_VERSION=$(sed -n 's/^[[:space:]]*version[[:space:]]*[:=][[:space:]]*//p' Cloudreve/manifest \
              | tr -d '"' | tr -d "'" | sed 's/^v//' | xargs)
            echo "本地manifest版本：$LOCAL_VERSION"
            echo "local_version=$LOCAL_VERSION" >> $GITHUB_OUTPUT
          else
            echo "❌ 未找到Cloudreve/manifest，强制触发更新"
            echo "local_version=0.0.0" >> $GITHUB_OUTPUT
            mkdir -p Cloudreve
            echo "version: 0.0.0" > Cloudreve/manifest
            git add Cloudreve/manifest
            git commit -m "chore: 初始化manifest文件" || echo "无需要提交的变更"
          fi

      - name: 5. 获取Cloudreve官方最新正式版版本号
        id: remote_ver
        run: |
          REMOTE_VERSION=$(curl -sSL --retry 3 --connect-timeout 10 \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/repos/cloudreve/Cloudreve/releases/latest" \
            | jq -r '.tag_name' | sed 's/^v//' | xargs)
          
          if [ -z "$REMOTE_VERSION" ] || [ "$REMOTE_VERSION" = "null" ]; then
            echo "❌ 获取远程版本失败"
            exit 1
          fi
          
          echo "远程最新版本：$REMOTE_VERSION"
          echo "remote_version=$REMOTE_VERSION" >> $GITHUB_OUTPUT

      - name: 6. 版本比对，判断是否更新
        id: compare
        run: |
          if [ "${{ steps.local_ver.outputs.local_version }}" = "${{ steps.remote_ver.outputs.remote_version }}" ]; then
            echo "✅ 本地版本与远程版本一致（${{ steps.remote_ver.outputs.remote_version }}），无需更新"
            echo "need_update=false" >> $GITHUB_OUTPUT
          else
            echo "🔄 版本不一致，开始更新打包流程"
            echo "need_update=true" >> $GITHUB_OUTPUT
          fi

      # ===================== 第三步：仅版本不一致时执行 =====================
      - name: 7. 下载Cloudreve Linux AMD64版本（极简匹配）
        if: steps.compare.outputs.need_update == 'true'
        id: download_cloudreve
        run: |
          set -euo pipefail
          
          # 1. 获取完整发行版数据
          RELEASE_DATA=$(curl -sSL --retry 3 --connect-timeout 10 \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/repos/cloudreve/Cloudreve/releases/latest")
          
          # 2. 调试：输出所有资产名称
          echo "📜 远程发行版所有资产名称："
          echo "$RELEASE_DATA" | jq -r '.assets[].name'
          
          # 3. 极简匹配：直接筛选包含 "linux_amd64.tar.gz" 的资产
          DOWNLOAD_URL=$(echo "$RELEASE_DATA" | jq -r \
            '.assets[] | select(.name | contains("linux_amd64.tar.gz")) | .browser_download_url' | head -n1)
          
          # 4. 最终校验
          if [ -z "$DOWNLOAD_URL" ] || [ "$DOWNLOAD_URL" = "null" ]; then
            echo "❌ 未找到包含 'linux_amd64.tar.gz' 的资产"
            exit 1
          fi
          
          # 5. 提取匹配到的资产名
          MATCHED_ASSET=$(echo "$RELEASE_DATA" | jq -r '.assets[] | select(.name | contains("linux_amd64.tar.gz")) | .name' | head -n1)
          echo "✅ 匹配到目标资产：$MATCHED_ASSET"
          echo "✅ 下载链接：$DOWNLOAD_URL"
          
          # 6. 创建目录并下载
          mkdir -p Cloudreve/app/bin
          curl -SL --retry 3 --connect-timeout 10 -o /tmp/cloudreve.tar.gz "$DOWNLOAD_URL"
          
          # 7. 解压并验证二进制文件
          tar -zxf /tmp/cloudreve.tar.gz -C /tmp
          if [ ! -f "/tmp/cloudreve" ]; then
            echo "❌ 解压后未找到cloudreve二进制文件"
            exit 1
          fi
          
          # 8. 移动文件+添加执行权限
          mv /tmp/cloudreve Cloudreve/app/bin/
          chmod +x Cloudreve/app/bin/cloudreve
          rm -rf /tmp/cloudreve.tar.gz /tmp/cloudreve
          
          # 9. 验证文件信息
          echo "✅ 下载完成，文件信息："
          ls -lh Cloudreve/app/bin/cloudreve
          file Cloudreve/app/bin/cloudreve

      - name: 8. 更新manifest的version字段
        if: steps.compare.outputs.need_update == 'true'
        run: |
          # 更新版本号
          sed -i -E \
            "s/(^[[:space:]]*version[[:space:]]*[:=][[:space:]]*)([\"']?)([0-9a-zA-Z.]+)([\"']?)/\1\2${{ steps.remote_ver.outputs.remote_version }}\4/" \
            Cloudreve/manifest
          
          echo "✅ manifest版本已更新为：${{ steps.remote_ver.outputs.remote_version }}"
          cat Cloudreve/manifest | grep -i version

          # 暂存变更
          git add Cloudreve/manifest

      - name: 9. 提交并推送manifest变更到当前仓库
        if: steps.compare.outputs.need_update == 'true'
        run: |
          git commit -m "chore: 更新Cloudreve版本至${{ steps.remote_ver.outputs.remote_version }}" || echo "✅ 无需要提交的manifest变更"
          git push origin HEAD:${{ github.ref_name }}
          echo "✅ manifest版本号已推送到当前仓库"

      - name: 10. 下载并安装fnpack（指定地址）
        if: steps.compare.outputs.need_update == 'true'
        run: |
          FNPACK_URL="https://static2.fnnas.com/fnpack/fnpack-1.0.4-linux-amd64"
          curl -SL --retry 3 --connect-timeout 10 -o /usr/local/bin/fnpack "$FNPACK_URL"
          chmod +x /usr/local/bin/fnpack
          
          if ! command -v fnpack &>/dev/null; then
            echo "❌ fnpack安装失败"
            exit 1
          fi
          echo "✅ fnpack安装完成：$(which fnpack)"

      - name: 11. 进入Cloudreve目录执行fnpack打包（核心修改）
        if: steps.compare.outputs.need_update == 'true'
        id: build_fpk
        run: |
          # 进入Cloudreve目录
          cd Cloudreve
          echo "🚀 进入Cloudreve目录，执行打包命令：fnpack build"
          fnpack build
          
          # 查找打包后的fpk文件（当前目录即Cloudreve）
          FPK_FILE=$(find . -name "*.fpk" -type f | head -n1)
          if [ -z "$FPK_FILE" ]; then
            echo "❌ 未找到打包后的fpk文件"
            exit 1
          fi
          
          # 拼接完整路径（便于后续复制）
          FPK_FULL_PATH=$(pwd)/$FPK_FILE
          # 重命名为固定名称 fpk-cloudreve.fpk
          FPK_FIXED_NAME="fpk-cloudreve.fpk"
          mv "$FPK_FULL_PATH" /tmp/$FPK_FIXED_NAME
          
          echo "✅ fnpack打包完成，fpk文件：/tmp/$FPK_FIXED_NAME"
          echo "fpk_file=/tmp/$FPK_FIXED_NAME" >> $GITHUB_OUTPUT
          echo "fpk_name=$FPK_FIXED_NAME" >> $GITHUB_OUTPUT

      # ===================== 第四步：推送fpk到FnDepot仓库 + 更新fnpack.json =====================
      - name: 12. 克隆FnDepot仓库（目标仓库）
        if: steps.compare.outputs.need_update == 'true'
        run: |
          # 使用PAT令牌克隆（需在当前仓库Secrets配置 FnDepot_PAT）
          git clone https://${{ secrets.FnDepot_PAT }}@github.com/moxyis/FnDepot.git /tmp/FnDepot
          echo "✅ 成功克隆FnDepot仓库到/tmp/FnDepot"

      - name: 13. 复制fpk文件到FnDepot仓库目标目录（覆盖旧文件）
        if: steps.compare.outputs.need_update == 'true'
        run: |
          # 创建目标文件夹（若不存在）
          mkdir -p /tmp/FnDepot/fpk-cloudreve
          
          # 先删除旧的fpk文件（避免冗余）
          rm -f /tmp/FnDepot/fpk-cloudreve/fpk-cloudreve.fpk
          
          # 复制新的fpk文件
          cp ${{ steps.build_fpk.outputs.fpk_file }} /tmp/FnDepot/fpk-cloudreve/
          
          echo "✅ fpk文件已复制到：/tmp/FnDepot/fpk-cloudreve/${{ steps.build_fpk.outputs.fpk_name }}"
          ls -lh /tmp/FnDepot/fpk-cloudreve/

      - name: 14. 更新FnDepot仓库的fnpack.json中fpk-cloudreve的版本号
        if: steps.compare.outputs.need_update == 'true'
        run: |
          cd /tmp/FnDepot
          
          # 检查fnpack.json是否存在，不存在则创建（基础结构）
          if [ ! -f "fnpack.json" ]; then
            echo '{"fpk-cloudreve": {"version": ""}}' > fnpack.json
            echo "✅ 创建默认的fnpack.json文件"
          fi
          
          # 使用jq工具更新版本号（核心：修改fpk-cloudreve对应的version）
          # 若fnpack.json结构不同，需调整jq表达式（示例：{"fpk-cloudreve": {"version": "x.x.x"}}）
          jq --arg ver "${{ steps.remote_ver.outputs.remote_version }}" \
            '.["fpk-cloudreve"].version = $ver' fnpack.json > fnpack.tmp && mv fnpack.tmp fnpack.json
          
          echo "✅ fnpack.json更新完成，当前内容："
          cat fnpack.json

      - name: 15. 提交并推送fpk文件和fnpack.json到FnDepot仓库
        if: steps.compare.outputs.need_update == 'true'
        run: |
          cd /tmp/FnDepot
          
          # 暂存所有变更（fpk文件 + fnpack.json）
          git add fpk-cloudreve/${{ steps.build_fpk.outputs.fpk_name }} fnpack.json
          
          # 提交变更（备注版本号）
          git commit -m "chore: 更新Cloudreve ${{ steps.remote_ver.outputs.remote_version }} fpk包及fnpack.json版本号" || echo "✅ 无需要提交的变更"
          
          # 推送变更到FnDepot仓库main分支（根据实际分支修改）
          git push origin main
          echo "✅ fpk文件和fnpack.json已推送到 https://github.com/moxyis/FnDepot/tree/main/fpk-cloudreve"

      # ===================== 可选：上传打包产物到Action Artifact =====================
      # - name: 16. 上传打包产物
      #   if: steps.compare.outputs.need_update == 'true'
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: cloudreve-build-${{ steps.remote_ver.outputs.remote_version }}
      #     path: Cloudreve/
      #     retention-days: 7
